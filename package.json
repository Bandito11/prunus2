{
  "name": "prunus2",
  "private": true,
  "version": "0.0.6",
  "description": "Abey app",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build",
    "docs": "stencil build --docs readme.md",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch",
    "electron-serve": "npm run build && npx cap copy && npx cap open electron"
  },
  "dependencies": {
    "@capacitor/android": "^1.2.0",
    "@capacitor/cli": "^1.2.0",
    "@capacitor/core": "^1.2.0",
    "@capacitor/ios": "^1.2.0",
    "@ionic/core": "^4.9.0",
    "capacitor-data-storage-sqlite": "^1.2.1",
    "lokijs": "^1.5.7"
  },
  "devDependencies": {
    "@stencil/core": "^1.4.0",
    "@types/jest": "^24.0.18",
    "@types/lokijs": "^1.5.2",
    "workbox-build": "^4.3.1"
  }
}
